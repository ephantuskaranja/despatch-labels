PORT=3100

# MSSQL connection (SQL Server)
DB_CLIENT=mssql
DB_HOST=127.0.0.1
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourStrong!Passw0rd
DB_NAME=despatch_labels

# Optional: force a specific scale baud rate instead of auto-detecting the
# profile list in lib/scale.js (leave blank to auto-detect).
SCALE_BAUD_RATE=
SCALE_FORCE_BAUD=false
